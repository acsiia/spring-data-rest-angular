1. Подключение к бд Postgresql в файле application.properties
   Есть 2 варианта
   1.1 использовать подключение к готовой бд расположенной на облаке Heroky, которое уже настроено.
   1.2 использовать собственную бд. Для этого ввести url, username, password своей бд в файле application.properties.
        А так же раскомментировать #spring.jpa.hibernate.ddl-auto=create для автоматического создания таблицы в бд при сборке проекта.
        После того как hibernate при сборке создаст таблицу в вашей бд, заполните её данными из файла db.sql.

   При выборе первого способа, будьте готовы что проект будет собираться дольше обычного.
   Я реккомендую второй способ (подключение к собственной бд), т.к. бд на облаке Heroku ограничена количеством
   одновременных подключений (connection), и из-за превышения этого количества вы не получите доступ к бд.

2. Сборка проекта и запуск
    Соберите проект выполнив команду с помощью maven spring-boot:run

3. Откройте http://localhost:8080/home